import React from "react";

// Footer component displays a footer section with provided content
const Footer = ({ content }) => {
  return (
    <footer>
      <p>{content}</p>
    </footer>
  );
};

export default Footer;
